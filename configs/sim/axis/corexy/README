ejemplos de corexy:

corexy_by_kins.ini
  Demuestra la cinemática corexykins
  (Nota: el uso de cinemática corexykins
   en hardware real requiere interruptores
   home activados por posiciones de motor)

corexy_by_hal.ini
  Demuestra el uso de trivkins con
  cinemática corexy implementada por un
  componente hal
  (Nota: este método admite
  interruptores de home alineados con x,y)

Notas:
 1) Las configuraciones comparten archivo de 
    configuración ini comun de corey_base.inc
 2) Los interruptores home son simulados
 3) los stepgens están configurados pero no
    incluyen las conexiones al hardware
    .
