demostración de hal-histograma

GUI Axis que inicia dos aplicaciones hal-histograma para pines:
  motion-command-handler.time
  halui.axis.x.pos-commanded

Normalmente, hal-histograma se ejecutaría desde la línea de comando.

Para su uso:
$ hal-histogram --help
